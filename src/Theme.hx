class Colors {
    public static var White(default, null) = 0xffffffff;

    public static var Main(default, null) = White;
}

class TextScales {
    public static var Normal(default, null) = 2;
    public static var Big(default, null) = Normal * 2;
    public static var Small(default, null) = Normal / 2;
}

class Margins {
    public static var Normal(default, null) = 10;
    public static var Big(default, null) = Normal * 2;
    public static var Small(default, null) = Normal / 2;
}